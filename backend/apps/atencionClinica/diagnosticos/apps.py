from django.apps import AppConfig

class DiagnosticosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.atencionClinica.diagnosticos'
    verbose_name = 'Diagnósticos'
