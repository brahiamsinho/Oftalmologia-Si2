from django.apps import AppConfig


class PreoperatorioConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.atencionClinica.preoperatorio'
    verbose_name = 'Preoperatorio'

