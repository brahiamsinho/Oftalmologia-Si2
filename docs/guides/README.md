# Guías (`docs/guides/`)

Material operativo para quien despliega o mantiene el entorno (no reemplaza `docs/ai/` para contexto de producto).

| Guía | Contenido |
|------|-----------|
| [despliegue-ubuntu-nube.md](despliegue-ubuntu-nube.md) | VM Ubuntu / Azure: Docker, **Compose v2** (`docker compose`), `git pull`, `migrate`, `seed`, NSG. |
